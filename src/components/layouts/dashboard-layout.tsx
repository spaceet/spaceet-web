import React from "react"

interface Props {
	children: React.ReactNode
}

export const DashboardLayout = ({}: Props) => {
	return (
		<main className="">
			<div className=""></div>
			<div className=""></div>
		</main>
	)
}
